{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd dashboard_app && npm install && npm run build",
  "outputDirectory": "dashboard_app/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
