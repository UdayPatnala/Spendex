# Optional: override the API base URL.
# If unset, the dashboard auto-detects:
#   - On localhost → http://localhost:8000/api
#   - On a deployed domain → /_/backend/api (Vercel routing)
# VITE_API_BASE_URL=https://your-backend.com/api
